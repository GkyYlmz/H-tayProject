using System.Collections.Generic;

namespace Hitay.Common.Helpers
{
    public static class NotificationHelper
    {
        //public static MvcHtmlString Notification(this HtmlHelper helper, IEnumerable<string> messages, NotificationTypes type, NotificationLayouts layout, NotificationThemes theme, NotificationCloseTypes closeType, int timeout, bool progressBar, bool modal);
    }
}
